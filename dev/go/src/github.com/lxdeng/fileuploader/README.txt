Provide a form to choose a file and a relative (to the default directory) target location. Upload the file to the location.

Start server:
  ./fileuploader --host=":8000"

In a browser:
http://localhost:8000/upload
