// Ski is a standalone object, since it does not have companion class
// a companion class shares the same name of a companion object, and is in the same source file
object Ski {
  def main(args: Array[String]) = {
    println("ski is fun")
  }
}
